{% extends 'base.html.twig' %}

{% block body %}
    <h1>Salespoint</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ salesPoint.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ salesPoint.name }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('salespoint_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('salespoint_edit', { 'id': salesPoint.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
