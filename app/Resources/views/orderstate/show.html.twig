{% extends 'base.html.twig' %}

{% block body %}
    <h1>Orderstate</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ orderState.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ orderState.name }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('orderstate_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('orderstate_edit', { 'id': orderState.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
