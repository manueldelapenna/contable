{% extends 'base.html.twig' %}

{% block body %}
    <h1>Salescondition</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ salesCondition.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ salesCondition.name }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('salescondition_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('salescondition_edit', { 'id': salesCondition.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
